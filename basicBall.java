import java.awt.Color;

public class basicBall extends Ball {

	public basicBall(int newid, int radius, int initXpos, int initYpos, int speedX, int speedY, int maxBallSpeed, Color color,
			Player player, GameWindow gameW) {
		super(newid, radius, initXpos, initYpos, speedX, speedY, maxBallSpeed, color, player, gameW);
	}

	
}
